# @package _global_

defaults:
  - _self_
  - datamodule: datamodule2d.yaml # choose datamodule with `test_dataloader()` for evaluation
  - model: gat.yaml
  - callbacks: default.yaml
  - logger: null
  - trainer: default.yaml
  - paths: default.yaml
  - extras: default.yaml
  - hydra: default.yaml
  
  - experiment: null

task_name: "eval"

tags: ["dev"]

# passing checkpoint path is necessary for evaluation
ckpt_path: ???
